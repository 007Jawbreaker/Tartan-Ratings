<%= form_for(@cafeteria_category) do |f| %>
  <% if @cafeteria_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cafeteria_category.errors.count, "error") %> prohibited this cafeteria_category from being saved:</h2>

      <ul>
      <% @cafeteria_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
